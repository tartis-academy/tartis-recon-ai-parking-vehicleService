# Perfil PROD: BD dedicada de este microservicio (database-per-service real).
# Detalle: Obsidian Tartis-Recon-IA.
spring.datasource.url=jdbc:postgresql://${VEHICLE_DB_HOST}:${VEHICLE_DB_PORT:5432}/${VEHICLE_DB_NAME:vehicle_db}
spring.datasource.username=${VEHICLE_DB_USER}
spring.datasource.password=${VEHICLE_DB_PASSWORD}

spring.jpa.hibernate.ddl-auto=validate

spring.devtools.restart.enabled=false
